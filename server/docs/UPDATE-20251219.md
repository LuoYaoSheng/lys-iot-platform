# 更新说明 - v20251219

**作者**：罗耀生
**日期**：2025-12-19
**版本**：v20251219-095250

---

## 🎯 更新内容

### 新增功能：数据库自动初始化

**问题背景**：
- 之前部署时，产品数据依赖 `init.sql` 文件在容器首次创建时自动初始化
- 但如果 MySQL 容器数据卷已存在，`init.sql` 不会执行
- 导致生产环境出现 `invalid_product_key` 错误

**解决方案**：
- ✅ 应用启动时自动检查产品表是否为空
- ✅ 如果为空，自动插入默认产品数据
- ✅ 真正实现"一键部署"，无需手动干预

### 代码变更

#### 1. 修改 `cmd/server/main.go`

**新增函数**：`initDefaultData(db *gorm.DB)`

```go
// initDefaultData 初始化默认数据（仅在数据为空时执行）
func initDefaultData(db *gorm.DB) error {
	log.Println("Checking default data...")

	// 检查产品表是否为空
	var productCount int64
	if err := db.Model(&model.Product{}).Count(&productCount).Error; err != nil {
		return fmt.Errorf("failed to count products: %w", err)
	}

	// 如果产品表为空，插入默认产品
	if productCount == 0 {
		log.Println("Product table is empty, initializing default products...")

		defaultProducts := []model.Product{
			{
				ProductKey:  "SW-SERVO-001",
				Name:        "智能开关(舵机版)",
				Description: "ESP32智能舵机开关，支持BLE配网和MQTT控制",
				Category:    "switch",
				Status:      1,
			},
		}

		for _, product := range defaultProducts {
			if err := db.Create(&product).Error; err != nil {
				log.Printf("Failed to create product %s: %v", product.ProductKey, err)
			} else {
				log.Printf("✓ Created product: %s (%s)", product.ProductKey, product.Name)
			}
		}

		log.Println("Default products initialized successfully")
	} else {
		log.Printf("Product table already has %d products, skipping initialization", productCount)
	}

	return nil
}
```

**启动流程修改**：

```go
// 自动迁移数据库表
if err := autoMigrate(db); err != nil {
	log.Fatalf("Failed to migrate database: %v", err)
}
log.Println("Database migrated")

// 初始化默认数据（新增）
if err := initDefaultData(db); err != nil {
	log.Printf("Warning: Failed to initialize default data: %v", err)
}
```

---

## 📦 Docker 镜像

**镜像版本**：
- `luoyaosheng/iot-platform-core:v20251219-095250`
- `luoyaosheng/iot-platform-core:latest`

**镜像大小**：53.9MB

---

## 🚀 如何更新

### 方法一：使用 deploy.sh 脚本（推荐）

```bash
# 1. 拉取最新代码
cd /path/to/iot-platform-core
git pull

# 2. 一键部署
bash deploy.sh
```

脚本会自动：
- 拉取最新镜像
- 启动服务
- 等待服务就绪

### 方法二：手动更新

```bash
# 1. 拉取最新镜像
docker pull luoyaosheng/iot-platform-core:latest

# 2. 停止并删除旧容器
docker compose -f docker-compose.simple.yml down iot-core

# 3. 启动新容器
docker compose -f docker-compose.simple.yml up -d iot-core

# 4. 查看日志，确认初始化成功
docker compose -f docker-compose.simple.yml logs -f iot-core
```

**期望日志输出**：
```
2025/12/19 09:51:34 Database migrated
2025/12/19 09:51:34 Checking default data...
2025/12/19 09:51:34 Product table is empty, initializing default products...
2025/12/19 09:51:34 ✓ Created product: SW-SERVO-001 (智能开关(舵机版))
2025/12/19 09:51:34 Default products initialized successfully
```

或者如果产品已存在：
```
2025/12/19 09:52:15 Checking default data...
2025/12/19 09:52:15 Product table already has 1 products, skipping initialization
```

---

## ✅ 验证更新

### 1. 检查产品数据

```bash
docker compose -f docker-compose.simple.yml exec mysql \
  mysql -uroot -p${DB_PASSWORD} iot_platform \
  --default-character-set=utf8mb4 \
  -e "SELECT product_key, name, category, status FROM products;"
```

**期望输出**：
```
+---------------+--------------------+----------+--------+
| product_key   | name               | category | status |
+---------------+--------------------+----------+--------+
| SW-SERVO-001  | 智能开关(舵机版)     | switch   |      1 |
+---------------+--------------------+----------+--------+
```

### 2. 测试设备激活

```bash
curl -X POST http://localhost:48080/api/v1/devices/activate \
  -H "Content-Type: application/json" \
  -d '{
    "productKey": "SW-SERVO-001",
    "deviceSN": "TEST-UPDATE-001",
    "firmwareVersion": "1.0.0",
    "chipModel": "ESP32-WROOM-32E"
  }'
```

**期望返回**：
```json
{
  "code": 0,
  "data": {
    "deviceId": "dev_xxx",
    "deviceSecret": "sk_xxx",
    "mqttBroker": "your-ip:42883",
    "mqttUsername": "dev_xxx",
    "mqttPassword": "xxx"
  }
}
```

### 3. APP 配网测试

1. 打开 APP，扫描设备
2. 连接设备 BLE
3. 输入 WiFi 信息
4. ✅ **不再提示 `invalid_product_key` 错误**
5. ✅ 配网成功

---

## 🔧 故障排查

### 问题 1: 更新后仍然提示 `invalid_product_key`

**检查步骤**：

```bash
# 1. 确认镜像版本
docker images | grep luoyaosheng/iot-platform-core

# 期望输出包含 v20251219-095250 或更新版本

# 2. 查看应用日志
docker compose -f docker-compose.simple.yml logs iot-core | grep -E "(Checking default data|Created product)"

# 3. 手动检查产品数据
docker compose -f docker-compose.simple.yml exec mysql \
  mysql -uroot -p${DB_PASSWORD} iot_platform \
  -e "SELECT COUNT(*) FROM products;"
```

### 问题 2: 容器无法启动

```bash
# 1. 查看容器状态
docker compose -f docker-compose.simple.yml ps

# 2. 查看详细日志
docker compose -f docker-compose.simple.yml logs iot-core

# 3. 检查端口占用
netstat -tulpn | grep -E "(48080|44306|47379|42883)"
```

---

## 📝 开发者信息

### 推送新镜像到 Docker Hub

如果您是项目维护者，需要推送新版本镜像：

```bash
# 1. 构建并推送（自动化脚本）
cd /path/to/iot-platform-core
bash scripts/build-and-push.sh

# 2. 手动推送
docker login
docker push luoyaosheng/iot-platform-core:v20251219-095250
docker push luoyaosheng/iot-platform-core:latest
```

**注意**：如果遇到网络问题（EOF 错误），请使用代理或稍后重试。

---

## 📚 相关文档

- [极简部署指南](./部署指南.md)
- [问题修复指南](./FIX-PRODUCT-KEY-ERROR.md)
- [EMQX 配置指南](./EMQX完整配置指南.md)

---

**最后更新**：2025-12-19
**维护者**：罗耀生
